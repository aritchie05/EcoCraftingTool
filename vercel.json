{
  "buildCommand": "ng build --configuration=$ENVIRONMENT",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Robots-Tag",
          "value": "$ROBOTS_TAG_VALUE"
        }
      ]
    }
  ]
}
